# AGENTS.md

本文件约束 Codex 后续修改 `xunotes-site` 项目时的工作方式。除非用户明确要求，否则必须遵守。

## 项目定位

`xunotes.com` 是个人知识库、研究记录和笔记入口，不是商业产品站点。修改时优先保持内容清晰、结构可理解、维护成本低。

## 修改原则

1. 不要引入前端框架、构建工具或复杂依赖；当前站点以原生 HTML、CSS、JavaScript 为主。
2. 不要为了视觉炫技改动布局。页面应服务于阅读、观察和长期整理。
3. 不要修改现有页面视觉效果，除非用户明确提出视觉调整要求。
4. 新增内容应同时考虑中文版和英文版；中文版内容为准，英文版跟随翻译。
5. 修改前先阅读相关文件，理解当前结构后再动手。
6. 保持文件职责清晰：数据放在对应数据脚本中，页面模板放在 HTML 中，样式放在 `styles.css` 中。
7. 不要把密钥、管理员口令、Cloudflare Token 或其他敏感信息写入仓库。
8. 不要删除用户已有内容，除非用户明确要求。
9. 对线上发布相关操作要先完成本地检查，再提交和发布。
10. 如果 GitHub 普通推送不可用，可以使用 GitHub 官方 API 发布，但必须说明原因。

## 内容规则

- 投资内容是个人观察记录，不构成投资建议。
- 技术内容以可复用、可操作、便于日后查找为目标。
- 生活内容可以保持轻量，优先保留真实记录。
- 首页只作为入口，不应承载过长正文。
- 二级页用于展示栏目下的条目列表或核心观察。
- 三级页用于承载单个条目的详细内容。

## 验证要求

每次修改后至少检查：

- JavaScript 语法是否通过。
- 新增页面链接是否可达。
- 中英文切换是否不会破坏页面。
- 若涉及留言服务，确认 Worker 和 D1 流程不被破坏。

## 发布要求

静态网站发布到 GitHub Pages，域名为 `www.xunotes.com`。留言服务发布到 Cloudflare Worker，数据库使用 Cloudflare D1。
